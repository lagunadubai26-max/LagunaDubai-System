# إعداد الطباعة الحرارية في Windows

## أبعاد الفاتورة
- **العرض**: 58mm
- **الطول**: 300mm

---

## 1. إعداد حجم ورق مخصص في Windows

1. اذهب إلى **Devices & Printers** (أو Printers & Scanners)
2. اختار طابعتك الحرارية (مثلاً Epson TM-T20) → **Print Server Properties**
3. تبويب **Forms** → ضع علامة على **Create a new form**
4. سميها `58x300`
5. Units: **Metric**
6. Width: `58mm`, Height: `300mm`
7. Save Form

ثم:
1. ارجع لـ **Printers & Scanners** → اختار الطابعة → **Printer Properties**
2. تبويب **General** → **Preferences**
3. **Advanced** → **Paper Size** → اختر `58x300`
4. OK → OK

> لو حجم الورق مش ظهر في القائمة، اعمل Restart للكمبيوتر أو Driver الطابعة.

---

## 2. توصيل طابعة USB

- وصّل الطابعة عبر USB
- Windows هينصبها لوحدها أو من CD/موقع الشركة
- دور على الجهاز في **Devices & Printers** وتأكد إنه **Default Printer** أو مسجل

---

## 3. إعداد الطابعات داخل النظام (من المتصفح)

1. افتح النظام من GitHub Pages
2. اذهب إلى صفحة **Settings** (الإعدادات)
3. تحت **Printer Settings**، اضغط **Add Printer**
4. اختار **USB**
5. اسم الطابعة (مثلاً كاشير أو مطبخ)
6. **For Kitchen?**:
   - اختار **No** → طابعة الفاتورة (الكاشير)
   - اختار **Yes** → طابعة المطبخ
7. **Save**

كرر لكل طابعة عندك.

---

## 4. إعداد الطباعة التلقائية

في نفس صفحة **Settings**:

- **Auto-print Receipt**: ON (تطبع فاتورة الكاشير تلقائياً بعد كل طلب)
- **Auto-print Kitchen Order**: ON (تطبع طلب المطبخ تلقائياً)
- **Number of Copies**: 1 (أو 2 لو عايز نسختين)

---

## 5. لو عايز طابعة WiFi

1. الطابعة تكون موصلة على نفس شبكة الراوتر (IP ثابت)
2. في النظام: Settings → Add Printer → **WiFi**
3. أدخل IP الطابعة (مثلاً 192.168.1.100)
4. Port: 9100 (الافتراضي لـ ESC/POS)
5. شغّل `printer-proxy-server.js` على جهاز الكاشير:
   ```
   node printer-proxy-server.js
   ```
6. في Settings → Proxy URL: `ws://localhost:9090`

---

## 6. اختبار الطباعة

- من صفحة **Settings** → **Test Print** جنب كل طابعة
- أو اعمل طلب عادي من صفحة المنيو واتأكد إن الفاتورة وطلب المطبخ يطبعوا صح
